"""MCP server stub — full implementation in Phase 2."""

from __future__ import annotations

from iridium_core import WorkspaceIndexer


def main() -> None:
    """Entry point for iridium-mcp-server (Phase 1 skeleton)."""
    print("iridium-mcp-server 0.1.0 (skeleton)")
    print("Phase 2: stdio MCP transport, validate_dependency, reachability_context tools")
    print("Core indexer available:", WorkspaceIndexer)


if __name__ == "__main__":
    main()
